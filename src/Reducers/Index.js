const initialState="";

export const LoginTheUser = (state=initialState, action) => {
    switch(action.type) {
        case "REGISTER" :
            return 
        default: 
            return state;
    }
}